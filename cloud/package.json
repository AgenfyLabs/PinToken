{
  "name": "pintoken-cloud",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "db:push": "prisma db push",
    "db:generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "nanoid": "^5.0.0"
  },
  "devDependencies": {
    "prisma": "^6.0.0"
  }
}
